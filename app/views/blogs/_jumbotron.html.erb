<div class="jumbotron p-4 p-md-5 text-white rounded bg-dark">
  <div class="col-md-6 px-0">
    <h1 class="display-4 font-italic"><%= blog.title %></h1>
    <p class="lead my-3"><%= truncate(blog.body, length: 100) %></p>
    <p class="lead my-3"><em><%= link_to('Edit', edit_blog_path(blog)) if logged_in?(:site_admin) %></em></p>
    <p class="lead mb-0"><a href="<%= blog_path(blog) %>" class="text-white font-weight-bold">Continue reading...</a></p>
  </div>
</div>
